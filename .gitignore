*.iml
.gradle/
/local.properties
.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
.kotlin/
local.properties
app/build/
app/schemas/
*.apk
*.aab

# Firebase project config — contains per-project identifiers, not secrets
# but still project-specific; each developer/user adds their own.
app/google-services.json
.firebaserc
